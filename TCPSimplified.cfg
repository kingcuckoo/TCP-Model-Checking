INIT
    Init

NEXT
    Next

\* INVARIANT
\*     connectionState \in States
INVARIANT
    Len(dataToSend) >= 0
INVARIANT
    Len(unacknowledgedData) >= 0
INVARIANT
    Len(receivedData) >= 0

CONSTANT 